# Organic search reports

The weekly workflow writes Search Console query opportunities to
`search-intent/` and SerpBear keyword history to `rank-tracking/`. Credentials
are supplied through GitHub Actions secrets and are never stored here.
