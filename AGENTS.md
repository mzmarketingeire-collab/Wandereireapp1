# Project continuity

Before continuing work, read `docs/PROJECT-STATUS.md`. It records the current phase,
user decisions, unfinished changes, connection status, and next actions. Read
`docs/IRELAND_MAP_PLATFORM_ARCHITECTURE.md` for the user's proposal; it is a target
architecture, not a description of features already implemented.

The user's latest budget instruction is €0 upfront and €0 ongoing. Do not enable
paid services or billing without explicit agreement. R2 activation is on hold;
do not activate it. This latest instruction supersedes earlier conditional
Cloudflare/R2 setup discussions. Design within verified free limits.

Preserve existing uncommitted work. Verify tools actually work before claiming an
external connection is available. Never record credentials, tokens, or environment
variable values in handoff files.

Update `docs/PROJECT-STATUS.md` after meaningful work or before handing off to a
new session. Separate completed work, verified checks, and unverified assumptions.
