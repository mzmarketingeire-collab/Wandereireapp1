---
scope: Wander Éire v1 architecture spine — full-stack: React+Vite PWA frontend, Supabase backend, MapLibre map, admin route
purpose: Build substrate for solo builder (Claude Code) to implement v1 consistently; validates/firms up brief's draft stack and data model
altitude: initiative→features (whole-system spine, single v1 build, no epics yet)
updated: 2026-08-06T11:13
---

- (version) MapTiler free tier (2026): 100k tile requests/month, no card required, 100MB free hosting — pauses (no overage bill) if exceeded. Fits v1 personal/close-circle scale.
- (version) Supabase free tier (2026): 500MB DB storage, 1GB file storage, 50MB max upload/file, 5GB egress, 50k MAU, unlimited API requests. Free projects PAUSE after 7 days inactivity, capped at 2 active free projects per org.
- (version) vite-plugin-pwa (Workbox-based) is the current standard PWA tooling for Vite; generateSW mode with workbox.runtimeCaching — CacheFirst for hashed static assets, StaleWhileRevalidate for app shell, NetworkFirst (short networkTimeoutSeconds) for API/Supabase calls.
- (version) Capacitor 7.x current (2026), compatible with React 19 / Vite — confirms brief's 'PWA now, Capacitor wrap later, same codebase' is still a live, current path.
- (version) Eircode/Irish address geocoding: no single official free API; eircode.dev and geocode.xyz offer free/low-volume Irish address+Eircode lookup, commercial options (Autoaddress, Loqate, Postcoder) exist for higher-volume/accuracy. Admin-only, low-volume use case (v1 build-out) fits a free-tier option.
- (decision) [ADOPTED from PRD §13] Frontend: React + Vite. Map: MapLibre GL JS over real coordinates, illustrated-look vector styling. Backend: Supabase (Postgres+PostGIS, Auth, Storage). Mobile: PWA-first v1, Capacitor wrap later, same codebase. Not re-litigated — PRD treats these as settled platform choices, not open questions.
- (decision) [ADOPTED from PRD §8/FR-18] RLS enforced in Supabase: users can only modify own ticks/saves/contributions; admin role check enforced server-side (Postgres RLS/policy), never merely a client-side route guard. Binds: every write-path table needs an RLS policy keyed to auth.uid(); admin routes/queries need a role check in policy, not just React route guarding.
- (decision) [ADOPTED from Brief] Draft data model entities: locations, user_ticks, user_saves, user_photos, comments, profiles — treated as a starting draft per PRD §0 (superseded in shape by PRD Glossary/FRs), not final schema. This run's job is to firm up columns, types, status/moderation fields, and constraints.
- (decision) AD: Pin data strategy — fetch ALL published locations client-side on load (single query, no PostGIS bbox/viewport querying in v1). Binds: locations.geom stored as PostGIS geography(Point) with GIST spatial index from day one (so bbox querying is a drop-in addition later); category filter/dim is a client-side operation over the already-fetched set, never a re-query. Prevents: one feature re-querying Supabase on pan/zoom while another assumes a fully-loaded client set — only one pattern exists. Revisit when location count grows enough that fetch-all response size/latency becomes real (rough trigger: >1000 published locations or payload >1MB).
- (decision) AD: Geocoding for admin address/Eircode→coordinates (FR-20) uses eircode.dev. Binds: admin location form's geocode path calls eircode.dev; map-click remains the always-available fallback per FR-20/EXPERIENCE.md geocode-failure state. Note: smoke-test eircode.dev's reliability/uptime before relying on it in the admin build phase — no SLA on a free third-party service; map-click fallback covers outage risk.
- (decision) AD: Moderation/soft-delete uses a shared status enum column pattern, not separate archive/queue tables. locations.status: published|archived. user_photos.status and comments.status: pending|approved|rejected. Binds: every moderatable/archivable table gets a status column of this shape; RLS policies and the public map/detail queries filter WHERE status = 'published'/'approved' consistently; admin moderation queue query is WHERE status = 'pending' across user_photos+comments. Prevents: one table using a boolean+timestamp pattern while another uses enum status — divergent query/RLS shapes.
- (decision) AD: Category is a lookup table (categories: id, name, color_hex, icon_key, sort_order), not a Postgres enum. Binds: locations.category_id FK references categories.id; admin can add a new category (e.g. Food, Stay) via an INSERT (through /admin or direct DB), no schema migration or app redeploy required. Prevents: hardcoding category lists in both DB enum and frontend, which would require synchronized migrations to add one.
- (decision) AD: Photo pipeline — client-side resize/compress (max ~2000px dimension, re-encoded JPEG, target ~1-2MB) before Supabase Storage upload, for both admin location photos and user contribution photos. Binds: all photo-upload UI (admin form, contribution add control) routes through one shared client-side compression step before calling Storage. Prevents: one upload path storing raw multi-MB originals while another compresses, causing inconsistent storage/egress costs.
- (decision) AD: TanStack Query (React Query) + supabase-js is the server-state layer for the whole app — every screen's data fetch and mutation (locations, ticks, saves, photos, comments, admin CRUD, moderation) goes through it, giving one shared caching/loading/error/optimistic-update shape. Local-only UI state (open sheet, active filter, form fields) stays in plain React state/Context, never TanStack Query. Prevents: screens built at different times each hand-rolling their own fetch+loading+optimistic-update logic.
- (decision) AD: Photo storage — two Supabase Storage buckets. location-photos: public read, admin-write only (RLS), holds admin-authored location photos, always public once the location itself is status='published'. user-contributions: private bucket; storage.objects RLS SELECT policy allows access only if the linked user_photos row has status='approved', OR requester = submitter (auth.uid() = user_photos.user_id), OR requester has role=admin. Binds: every photo URL the app renders must resolve through this policy (signed/authenticated fetch for user-contributions, plain public URL for location-photos) — no single public bucket for both. Prevents: a pending user photo being publicly fetchable by URL before admin approval (closes a real FR-16/§9-privacy gap).
- (decision) [ADOPTED, low-risk, decided without separate elicitation — consistent with prior ADs] Anonymous Gate (FR-8) tracked client-side via localStorage: an array of distinct viewed location IDs on that browser/device; gate triggers when a NEW distinct id would be the 4th. Search (FR-5) is a client-side case-insensitive substring match over the already-fetched full location set (consistent with the fetch-all pin strategy AD) — no server-side full-text search needed at v1 scale. Distance (Glossary) stored as a single nullable text field (distance_display, e.g. '2.4km loop'), matching the Cost Amount pattern of allowing a qualifier rather than a bare number. Comment length capped at 500 chars via a DB check constraint (PRD FR-15 placeholder confirmed as-is); no per-location/per-user count cap on comments or photos in v1 (abuse is bounded by admin's manual moderation review, not a hard cap) — PRD FR-14/FR-15 assumptions confirmed as final for v1, not just placeholders.
- (decision) [ADOPTED, low-risk] Hosting: Vercel (git-based preview deploys, first-class Vite support; Netlify would work equally well — not a load-bearing choice, picked for simplicity of one fewer decision). First admin bootstrapped by the builder directly setting profiles.role='admin' via the Supabase SQL editor — no self-serve admin-promotion UI in v1 (solo builder, single admin at launch).
- (decision) AD: PWA offline caching scope (vite-plugin-pwa/Workbox, generateSW mode) — app shell + static assets: precached, StaleWhileRevalidate on update. Locations dataset fetch: NetworkFirst with a short timeout, falling back to the last cached response so the map still renders (last-known data) with no signal. Map tiles: CacheFirst, runtime-cached opportunistically as the user browses (capped via workbox expiration — e.g. maxEntries ~500, 30-day maxAgeSeconds), never pre-cached for the whole country. No offline write queue for ticks/saves/uploads (already fixed by FR-17 non-goal) — reads degrade gracefully offline, writes simply fail with a retry prompt per EXPERIENCE.md's stated failure copy.
- (decision) AD: County is a lookup table (counties: id, name — 32 seeded rows for Ireland, no border distinction per PRD §3), locations.county_id FK. Binds: Profile's 'X of 32 counties' progress stat (EXPERIENCE.md) is a COUNT(DISTINCT county_id) over the user's ticked locations joined to counties — trivial and typo-proof against a lookup table; would be error-prone string matching against free text. Same rationale/shape as the categories AD.
- (decision) AD: profiles row auto-provisioned via a Postgres trigger on auth.users INSERT (standard Supabase pattern) — creates profiles(id, role='user', display_name=NULL) at signup. Binds: app code never manually inserts a profiles row; role is only ever changed by direct DB access (admin bootstrapping AD) or a future admin-promotion tool, never client-writable (RLS: profiles.role is not in the user's own allowed UPDATE columns).
- (decision) AD: Location coordinates stored as geography(Point, 4326) (PostGIS geography type, WGS84) with a GIST spatial index — set now even though v1's query pattern is fetch-all (AD above), so distance/bbox queries are additive later, not a migration. geography over geometry: correct meters-based distance semantics out of the box if/when 'nearby' features are added post-v1, negligible cost difference at this scale.
- (decision) AD: Account deletion (GDPR, PRD §9) cascade behavior — user_ticks and user_saves rows are hard-deleted (pure personal data, no reason to retain). Pending/unapproved user_photos and comments are hard-deleted along with their Storage objects. Already-APPROVED (publicly visible) user_photos/comments are anonymized, not deleted: user_id set to NULL (ON DELETE SET NULL, column made nullable), content and photo file retained — matches PRD §9's 'where feasible' language: deleting an approved photo would break a location's already-public detail page for other users, so the personal link is severed instead of the content destroyed. profiles row and the auth.users record are hard-deleted. Revisit if legal/GDPR review later requires full erasure even of anonymized public content.
- (question) OPEN: whether eircode.dev requires an API key. If yes, the key must not ship in the client bundle — geocode calls would need to proxy through a Supabase Edge Function (server-side secret) rather than calling eircode.dev directly from the admin browser client. If eircode.dev is keyless/public, direct client-side calls are fine. Not resolved by judgment — resolve by checking the actual service during admin-form implementation; default to the Edge Function proxy shape if in doubt (cheap to build, closes the leak risk either way).
- (decision) AD: No custom backend service — Supabase (Postgres+PostGIS, Auth, Storage, RLS) is the entire backend; the React app talks to it directly via supabase-js. The only carve-out is a possible thin Supabase Edge Function for geocode key-hiding (see open question on eircode.dev). Binds: no Express/Node/etc. API layer gets introduced for CRUD, ticks/saves, contributions, or admin operations — those are direct supabase-js calls gated by RLS, consistent with the RLS-is-the-security-boundary AD.
- (decision) AD: Schema (tables, RLS policies, triggers, storage bucket policies) is version-controlled as Supabase CLI migration files (supabase/migrations/*.sql) — the source of truth, applied via /CI, never hand-edited live in the Supabase dashboard as the primary authoring path. Binds: every schema/RLS change ships as a migration file in the repo. Prevents: RLS policy drift between what's actually enforced in the cloud project and what the repo/spine says is enforced — a real risk given how much of this app's security lives in RLS, not app code.
- (decision) [ADOPTED, practical default] Single Supabase project used for both development and production pre-launch (free tier caps at 2 active projects and pauses after 7 days idle — splitting dev/prod now adds ops overhead a solo pre-launch builder doesn't need). Local schema iteration via Supabase CLI local dev (Docker), migrations pushed to the one cloud project. Revisit (split to a separate prod project) at or before the public-launch milestone referenced in PRD §1/§7.
- (event) Correction: prior migrations AD entry lost 'supabase db push' text to shell backtick substitution when logged. Intended text: schema changes are version-controlled as Supabase CLI migration files (supabase/migrations/*.sql), applied via the Supabase CLI (supabase db push) and CI, never hand-edited live in the dashboard as the primary authoring path. Decision content unaffected, only the logged wording.
- (event) Reconcile pass (4 parallel checks vs PROJECT-BRIEF.md, PRD, DESIGN.md, EXPERIENCE.md) complete. DESIGN.md: clean (1 minor non-blocking note on future low-contrast categories). Brief, PRD, EXPERIENCE.md: real gaps found, logged as new ADs below.
- (decision) AD: Admin-authored location photos are a child table, not an array column — location_photos(id, location_id FK, storage_path, sort_order, created_at), stored in the location-photos bucket (AD-11). Binds: brief's draft admin_photos[] field is realized as this table, not a Postgres array column. Prevents: array-column photo storage, which makes individual deletion/reordering awkward and breaks the lookup-table-for-lists consistency already set by categories/counties. Reconciles a gap the PROJECT-BRIEF.md reconcile check found: the spine's ERD had no representation for admin location photos at all.
- (decision) AD: Photo upload input constraints (resolves PRD §15 FR-14 assumption) — accepted input MIME types: image/jpeg, image/png, image/heic, image/webp (covers default iPhone/Android camera output); hard input-size cap 15MB per file, rejected client-side with inline error before the compress step (AD-9) even runs; no per-location or per-user count cap on photos in v1 (PRD's own placeholder confirmed as final, not just provisional) — moderation-volume risk against the single-admin FR-23 queue is accepted for v1 and noted in Deferred, not solved architecturally.
- (decision) AD: Comment length capped at 500 characters (resolves PRD §15 FR-15 assumption) via a DB CHECK constraint on comments.text plus a matching client-side maxlength — confirmed as final for v1, not a placeholder. No per-location/per-user comment count cap in v1, same rationale/risk-acceptance as the photo count-cap decision above.
- (decision) AD: Photo-upload consent tracked via profiles.photo_upload_consent_at (nullable timestamptz), set once on first upload (or at signup) — matches EXPERIENCE.md Flow 3's 'one-time upload-consent acknowledgment... not re-shown per submission.' Resolves a PRD §9 gap the reconcile check found: consent-at-time-of-upload had no data-layer representation anywhere in the spine.
- (decision) AD: Privacy policy is a static route (/privacy), statically-rendered content (no CMS/DB table), linked from the auth modal footer and profile menu per EXPERIENCE.md's IA table. Resolves a PRD §9 gap the reconcile check found: the spine's source tree never included a privacy-policy surface at all.
- (decision) AD: Pending gated action survives the Google OAuth redirect via sessionStorage, not plain React state. Before calling supabase.auth.signInWithOAuth (a full-page redirect to Google and back), the app writes a small pending-action record (e.g. {type: 'view_detail'|'tick'|'save', location_id}) to sessionStorage; the auth callback route reads and clears it on return, then executes the resumed action. Email/password sign-in (in-modal, no navigation) does not need this — plain React state survives fine there. Binds: EXPERIENCE.md Flow 1's climax step ('4th location's Detail view opens automatically' after Google sign-up). Prevents: AD-10's 'local UI state stays in plain React state/Context' rule silently breaking the one flow (OAuth redirect) where plain state doesn't survive a full page navigation — a real gap the EXPERIENCE.md reconcile check found.
- (decision) Extend AD-11 (photo storage): the same three-way access rule (approved OR own OR admin) must also be the table-row RLS SELECT policy on user_photos and comments themselves, not just the storage.objects policy on the user-contributions bucket — otherwise a submitter's own pending row wouldn't even be queryable to render their 'Pending review' inline state. Found by EXPERIENCE.md reconcile check as an under-specified (implied-only) rule.
- (decision) Extend AD-1 (platform stack): explicit rule that /admin is a route inside the same app/deployment/repo as the public app — never a separate app, build, or deployment target. Found by PROJECT-BRIEF.md reconcile check as a brief requirement stated explicitly but never asserted as a spine rule (source tree already complied by construction, but nothing would have flagged a violation).
- (note) Build sequencing (PRD §6.1's 3 phases) is owned by epics/stories, not this spine, but reconciling it here: nothing in the spine blocks phase 1 (map + admin CRUD, no accounts) shipping before phase 2 (auth + tracking + contributions) — RLS policies allow anonymous SELECT on published locations without requiring signup; auth-dependent tables (user_ticks, user_saves, user_photos, comments, profiles trigger) simply go unused until phase 2 wires up Supabase Auth. Phase 3 (PWA polish + Capacitor) only touches AD-1/AD-18, already scoped for that later pass. Found by PROJECT-BRIEF.md reconcile check as unaddressed.
- (note) PRD §5 non-goal 'no content moderation automation (AI flagging, profanity filters)' restated explicitly in the spine's Consistency Conventions / Deferred, not just left uncontradicted — found by PRD reconcile check as an echoed-elsewhere pattern (leaderboard/offline/native-build non-goals) this one broke.
- (event) Reviewer gate complete: rubric walker, web-verification, adversarial-incompatibility, all 3 as parallel subagents against ARCHITECTURE-SPINE.md. Full reviews in reviews/review-{rubric-walker,web-verification,adversarial}.md. Findings triaged below, applying fixes before finalize.
- (decision) AD-5 REVISED: eircode.dev rejected after web-verification found it's a single-commit ~11-star hobby Cloudflare Worker (credited to 'Lovable' AI app builder) that proxies Google Geocoding API via RapidAPI, and DOES require an API key — not the free/keyless service assumed. Replaced with Nominatim (OpenStreetMap's official geocoder, nominatim.openstreetmap.org): free, keyless (eliminates the key-hiding problem entirely, no Edge Function needed for v1), official OSM Foundation service, suitable for admin-only low-volume lookups (rate limit ~1 req/sec, acceptable for occasional admin use; usage-policy requires attribution, which is fine for an internal admin tool). MapTiler Geocoding API (same vendor as tiles) noted as a fallback in Deferred if Nominatim's Ireland/Eircode accuracy proves insufficient during implementation.
- (version) Web-verification corrections: Vite current line is 8.x (not 5/6.x as stated) as of 2026-08. Capacitor current is 8.x (8.5.0, July 2026), not 7.x — lower urgency since Capacitor is a later-phase concern, but the Stack table pin is corrected now. Supabase and MapTiler free-tier figures, TanStack Query v5, supabase-js v2.x, React 19.x all confirmed accurate, no change.
- (decision) Rubric-walker fix: AD-11's storage.objects RLS policy needs an explicit path convention to correlate a Storage object back to its user_photos row (the original Rule assumed this correlation without specifying it, making it unimplementable as written). Fix: user-contributions bucket objects are stored at path user-contributions/{location_id}/{user_photos.id}.{ext} — the RLS policy extracts user_photos.id from the object path (second path segment minus extension) and joins to check status/owner. Same path-correlation convention applies to location-photos/{location_id}/{location_photos.id}.{ext} for AD-21, though that bucket is simply public-read so no RLS join is needed there.
- (decision) Rubric-walker fix: FR-7 (copyable address/Eircode on every Detail Sheet) had no governing AD or ERD field. Fix: locations.address_display (text, admin-entered) — always populated regardless of which coordinate-entry path (AD-5 geocode vs map-click) admin used, since map-click captures no address at all. New AD-27 covers this plus the same gap found for Cost Type/Amount (FR-6/FR-19), which AD-19 referenced as an established pattern that was never actually specified: locations.cost_type (enum: free|paid) and locations.cost_amount (nullable text, allows a qualifier like '€10 parking' per PRD Glossary).
- (decision) Rubric-walker fix: corrected a broken cross-reference — AD-14's mention of '(AD-16)' for direct-DB-access role changes should read AD-17 (single Supabase project / admin bootstrap via SQL editor). AD-16 is the GDPR account-deletion cascade, unrelated.
- (decision) Adversarial fix (CRITICAL): AD-6 never stated the locations table's own SELECT RLS policy explicitly, unlike AD-11 which does this correctly for user_photos/comments. A literal implementation (status='published' only, no admin bypass) silently breaks FR-22's admin restore flow — admin's own location-list query would return zero archived rows. Fix: locations SELECT RLS is explicitly status='published' OR role='admin' (admin sees all statuses; public/anon sees published only).
- (decision) Adversarial fix (HIGH): AD-11's three-way condition (approved OR own OR admin), read literally, leaves a submitter's REJECTED photo/comment still fetchable by them — contradicting EXPERIENCE.md's 'no separate rejected notice, removed from queue and no longer shown as pending in the submitter's own view.' Fix: tighten AD-11's condition to status='approved' OR (status='pending' AND auth.uid()=user_id) OR role='admin' — rejected rows are admin-only (audit trail), invisible to the submitter and public alike, both at the table-row and storage-object RLS layers.
- (decision) Adversarial fix (HIGH): AD-14 described role-change protection as an RLS outcome without naming an enforcement mechanism — plain row-level RLS can't natively prevent a user's own UPDATE from also changing their own role column via a same-row policy. Fix: a BEFORE UPDATE trigger on profiles (e.g. prevent_role_self_change) that raises an exception if NEW.role <> OLD.role and the requester is not already role='admin' — enforced at the trigger layer, not assumed as an RLS side-effect.
- (decision) Adversarial fix (MEDIUM): no TanStack Query key convention was stated, risking cache collisions (e.g. map's published-only locations query vs admin's all-statuses locations query sharing a key) and undefined invalidation contracts for tick/save mutations. Fix: query keys are namespaced [entity, scope, ...params] — e.g. ['locations','published'] vs ['locations','admin-all']; ['ticks', userId], ['saves', userId]. Tick/save mutations invalidate their own user-scoped key on success/settle. Added to Consistency Conventions.
- (decision) Adversarial fix (MEDIUM): AD-9/AD-21 didn't state upload/insert ordering for the admin location-creation flow, where Flow 2 uploads photos (step 5) before Submit creates the location (step 6) — but location_photos.location_id is a NOT NULL FK and FR-21 forbids a draft/staged state. Fix: photo files are staged client-side (compressed, held in memory/object URLs) during form-fill; actual Storage upload + location_photos row insert happens only after the location row itself is successfully inserted, using its new id — as one sequenced client-side operation following the location INSERT, not a separate upload-first step.
- (event) All reviewer-gate fixes applied to ARCHITECTURE-SPINE.md (AD-5 geocoding swap, AD-6/AD-11 RLS tightening, AD-14 trigger enforcement, AD-21 upload ordering, new AD-27 for address/cost fields, Stack version corrections, query-key convention). Re-lint clean (0 high/critical, 10 low false-positive path-template flags). Spine finalized.
- (event) spine finalized
